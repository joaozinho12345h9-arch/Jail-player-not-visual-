local _0x5a2f = function(str)
    local s = ""
    for i = 1, #str do
        s = s .. string.char(str:byte(i))
    end
    return s
end

local _0x12e4 = [[
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "Admin Hub",
   LoadingTitle = "Jail System",
   LoadingSubtitle = "by Lua Gen",
   ConfigurationSaving = { Enabled = false }
})

local Tab = Window:CreateTab("Control", 4483362458)
local TargetName = ""

local function _0x99(playerName)
    local target = nil
    for _, p in pairs(game.Players:GetPlayers()) do
        if string.find(p.Name:lower(), playerName:lower()) then
            target = p
            break
        end
    end
    if target and target.Character then
        local hrp = target.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(0, -500, 0)
            hrp.Anchored = true
        end
    endend

local function _0x88()
    for _, p in pairs(game.Players:GetPlayers()) do
        if p.Backpack then
            for _, item in pairs(p.Backpack:GetChildren()) do
                if item:IsA("Tool") then
                    local r = item:FindFirstChildOfClass("RemoteEvent")
                end
            end
        end
    end
end

Tab:CreateInput({   Name = "Player Name",
   PlaceholderText = "...",
   RemoveTextAfterFocusLost = false,
   Callback = function(t) TargetName = t end,
})

Tab:CreateButton({
   Name = "Jail",
   Callback = function() _0x99(TargetName) end,
})_0x88()
game.Players.PlayerAdded:Connect(function() _0x88() end)
]]

loadstring(_0x12e4)()
